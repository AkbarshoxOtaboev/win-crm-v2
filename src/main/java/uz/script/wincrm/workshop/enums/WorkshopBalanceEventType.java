package uz.script.wincrm.workshop.enums;

public enum WorkshopBalanceEventType {
    /** Next workshop accepted the job (start). */
    ACCEPT,
    /** Workshop finished work (redirect or complete). */
    FINISH
}
